module Config {
    export var GAME_WIDTH: number = 800;
    export var GAME_HEIGHT: number = 500;

    export var RENDER_WIDTH: number = GAME_WIDTH / 2;
    export var RENDER_HEIGHT: number = GAME_HEIGHT / 2;
}